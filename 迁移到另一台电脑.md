# 迁移到另一台电脑

## 需要准备

- Windows 10 或 Windows 11
- 网络连接（只在首次安装时需要）
- Python 3.11 或更高版本。安装时勾选 `Add python.exe to PATH`。

## 迁移步骤

1. 在原电脑关闭分析器服务窗口。
2. 将整个项目文件夹复制到新电脑。若要保留历史研究，务必包含 `data` 文件夹；若不需要历史记录，可不复制其中的 `stock_chain.db`。
3. 在新电脑双击 `首次安装.cmd`。它会在项目内创建运行环境并安装所需组件。
4. 安装完成后，双击 `创建桌面快捷方式.cmd`，再使用桌面的快捷方式启动。也可以直接双击 `启动分析器.cmd`。
5. AI 平台密钥不会随项目迁移。请在新电脑的 AI 分析区域重新粘贴自己的密钥。
6. 如配置了 Tushare，请在新电脑重新设置 `STOCK_CHAIN_TUSHARE_TOKEN`；不设置也可正常使用基础功能。

## 建议的复制内容

复制整个项目根目录，但不必复制 `work\.venv`。新电脑会由首次安装脚本重新创建它。

不要把自己的 API Key 写入任何项目文件或备份文件。

